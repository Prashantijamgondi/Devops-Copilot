[phases.setup]
nixPkgs = ["python312"]

[phases.install]
skip = true
cmd = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python -m alembic upgrade head && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
